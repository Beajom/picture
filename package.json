{
  "name": "amazon-supply-workbench-cloudflare",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "db:init": "wrangler d1 execute amazon-supply-workbench --remote --file=./schema.sql && wrangler d1 execute amazon-supply-workbench --remote --file=./seed-current-data.sql",
    "deploy": "npm run db:init && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}