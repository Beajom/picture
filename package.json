{
  "name": "amazon-supply-workbench-cloudflare",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler pages dev . --d1 DB=amazon-supply-workbench --r2 IMAGES=amazon-supply-images",
    "deploy": "wrangler pages deploy ."
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}