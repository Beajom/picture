{
  "name": "amazon-supply-workbench-cloudflare",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "db:init": "wrangler d1 execute DB --remote --file=./schema.sql && wrangler d1 execute DB --remote --file=./seed-current-data.sql",
    "deploy": "npm run db:init && wrangler deploy"
  },
  "cloudflare": {
    "bindings": {
      "DB": {
        "description": "Amazon Supply Workbench 主数据库（D1）。首次部署会自动创建并导入现有商品、库存、供应商和辅料数据。"
      },
      "IMAGES": {
        "description": "商品父体图、颜色图和辅料图的 R2 存储桶。"
      }
    }
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}